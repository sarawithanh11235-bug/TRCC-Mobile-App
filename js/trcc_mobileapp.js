// toggle hamburger menu when hamburger icon is clicked
function hamburgerFunction() {
    document.getElementById("hamburgerDropdown").classList.toggle("show");
}

// go back to the previous page when "back" is clicked
function goBack() {
    window.history.back();
}

